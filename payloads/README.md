# Test Payloads

Different prompt injection payloads to test bot vulnerability.
Ordered from obvious to sneaky.

## Usage

Post these on bot social networks or send to bots for testing.
Replace `YOUR_BOT_NAME` and `YOUR_PLATFORM` with actual values.
Replace `localhost:5555` with your actual honeypot URL when deploying.
